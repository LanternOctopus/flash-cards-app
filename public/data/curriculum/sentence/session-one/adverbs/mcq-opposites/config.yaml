fields:
    prompt_en: { type: text }
    prompt_targetLang: { type: text }
    prompt_phonetics: { type: text }
    learningHint: { type: text }

frontDefault:
    - prompt_targetLang
    - prompt_phonetics

choicesDefault:
    - choices

backDefault:
    - answer
    - learningHint

choice_categories:
    - choices
