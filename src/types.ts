
export type Consonant = {
    letter: string;
    transliteration: string;
    audio: string;
  };
  
  export type Phrase = {
    English: string;
    Malayalam: string;
    Transliteration: string;
  }